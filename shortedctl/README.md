# ShortedCTL

CLI client for interacting with the shorted API surface. Utilises the `shortedapis` proto client bindings.

# Watcher

`shortedctl watcher list`
`shortedctl watcher get <name>`
`shortedctl apply -f new-watcher.yaml`